import 'package:flutter/material.dart';

class Final_details_project extends StatelessWidget {
  const Final_details_project({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
